package com.revature.codechallenge;

public static int MinimumMutations {

	    //scanner input, assuming the same number of characters for start and end will give OutOfBounds if not
	    Scanner scan = new Scanner(System.input);
	    int input = 1;
	    
	    String str = "";
	    system.out.println("start: ");
	    str = scan.nextLine();
	            
	    //setting a bank
	    String[] geneBank = {"AACCGGTT", "AACCGGTA"};
	   
	    String end = "";
	        System.out.print("end: ");
	            end= scan.nextLine();
	            
	    //verifying the end gene is a proper gene in the gene bank
	    boolean insideBank = false;
	            
	        //this loop checks if the end gene is in the gene bank
	          for (int i =0; i < geneBank.length; i++) {
	              if (geneBank[i].equals(end))
	              {
	                  insideBank = true;
	              }
	          }
	          //went through gene bank, didn't find end gene in gene bank
	          if (insideBank == false) {
	              return -1;
	              
	          //did find end gene in gene bank, compares start and end using a counter
	          //character to character because of charAt
	          //if it finds a difference, it adds it to counter
	          } else {
	              int count = 0;
	              for (int i = 0; i <str.length(); i++) {
	                  if (str.charAt(i) != ed.charAt(i)){
	                      count++;
	                  }
	              } return count;
	              
	          }
	
	public static void main(String[] args) {
	    int ans = MinimumMutations();
	    System.out.println(ans);
	 };

}
